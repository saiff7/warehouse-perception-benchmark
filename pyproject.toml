[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "warehouse_perception"
version = "0.1.0"
description = "Warehouse perception reliability benchmark for cluttered robotic picking"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
